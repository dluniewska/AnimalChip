
<div class="card-header">

    <div class="row">
        <div class="col-md-12  margin-tb mt-4">
            <div class="row justify-content-center" style="margin-bottom: 10px;">
                <p>
                    <h1 class="display-4">Search for Animal</h1>
                </p>
            </div>
        </div>
    </div>
</div>

@TempData["MessageNotNumeric"]

<hr />
<div class="card-body">
    <div class="container">
        <div class="col-sm12">
            <form asp-action="ShowResultsAdvanced">

                <div class="form-group">
                    <label for="SearchPhrase" class="control-label"></label>
                    <input name="SearchPhrase" class="form-control" />
                </div>

                <div class="form-group">
                    <input type="submit" value="Search" class="btn btn-secondary" />

                    @if (User.IsInRole("Administrator"))
                    {
                        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
                    }
                    else
                    {
                        <a asp-action="IndexYourAnimal" class="btn btn-secondary">Back to List</a>
                    }
                </div>
            </form>
        </div>
    </div>
</div>

    @*<div>
        <a asp-area="" asp-controller="Home" asp-action="Index" class="btn btn-secondary">Back to Home Page</a>
    </div>*@
